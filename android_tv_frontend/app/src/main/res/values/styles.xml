<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- PUBLIC_INTERFACE -->
    <!-- App theme extending Material3 to avoid missing variant errors -->
    <style name="AppTheme.Material3.DayNight.NoActionBar" parent="Theme.Material3">
        <!-- Prefer no action bar behavior -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- TV-specific defaults -->
        <item name="android:windowIsTranslucent">false</item>
    </style>
</resources>
