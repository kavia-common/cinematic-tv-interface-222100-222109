# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true
# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official

# Avoid failing unit test tasks when no tests are discovered for a given variant.
# This is needed because Gradle may create additional test tasks (e.g., debugUnitTest)
# that can discover zero tests depending on filters or source set setup.
# See: https://issuetracker.google.com/issues/237030779
android.testConfig.failOnNoTests=false
